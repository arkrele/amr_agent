# 快速启动

## 1. 安装

在项目根目录打开终端：

```bash
pip install -e .
```

## 2. 配置 API

编辑项目根目录的 `.env`，填入：

```
OPENAI_API_KEY=你的key
OPENAI_BASE_URL=https://api.deepseek.com
```

## 3. 运行

### 方式 1: 交互式（最简单）

双击 `workspace\run.bat`，或终端执行：

```bash
cd workspace
python launch.py
```

按提示输入问题描述，选择求解器方式即可。

### 方式 2: 一行命令

```bash
cd workspace
python launch.py --prompt "你的物理问题描述"
```

### 方式 3: 用已有配置

先编辑好 `workspace\problem.yaml` 指向你的求解器和网格，然后：

```bash
cd workspace
python launch.py --config problem.yaml
```

---

## 用户需要准备的三种情况

### 情况 A: 你有自己的求解器

编辑 `workspace\problem.yaml`，设置：

```yaml
solver:
  type: "user_template"
  path: "./my_solver.py"
```

保证你的求解器支持以下调用：

```bash
python my_solver.py --mesh <网格目录> --output-dir <输出目录> --params '<json参数>'
```

必须是 `<输出目录>/metrics.json`。

### 情况 B: 让 Agent 自己写

```yaml
solver:
  type: "agent_generated"
  path: ""
```

### 情况 C: 直接看效果（什么都不用准备）

```bash
mesh-agent run -p tests\test_convection_diffusion\problem.yaml -o test_output
```
