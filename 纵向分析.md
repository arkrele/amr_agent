# AI 自主科研 Agent 设计：六大核心维度技术总结

> 覆盖 15 个系统 | 跨 Nature / ICLR / EMNLP / arXiv
> 分析维度：搜索与探索 / 质量验证与筛选 / 记忆管理 / 实验代码编排 / 并行与效率 / 预算管理
> 日期：2026-07-20

---

## 系统索引

| #   | 系统                     | 来源              | 发表                  | 类型          |
| --- | ---------------------- | --------------- | ------------------- | ----------- |
| 1   | **Re⁴**                | 中科院力学所          | ICLR 2026 Workshop  | 科学计算 Agent  |
| 2   | **ATHENA**             | 布朗大学            | arXiv 2025          | 科学计算 Agent  |
| 3   | **GRAFT-ATHENA**       | 布朗大学            | arXiv 2026          | 跨问题迁移       |
| 4   | **Sibyl-AutoResearch** | 悉尼大学            | arXiv 2026          | 自演化 Harness |
| 5   | **EviBound**           | 康奈尔大学           | arXiv 2025          | 防幻觉门控       |
| 6   | **ResearchLoop**       | —               | arXiv 2026          | 证据链状态机      |
| 7   | **AI Scientist v1**    | Sakana AI       | Nature 2026         | 端到端科研       |
| 8   | **AI Scientist v2**    | Sakana AI       | ICLR 2025 Workshop  | 树搜索科研       |
| 9   | **DeepScientist**      | 西湖大学            | arXiv 2025          | 前沿推进        |
| 10  | **AI Co-Scientist**    | Google DeepMind | Nature 2026         | 假设锦标赛       |
| 11  | **Agent Laboratory**   | AMD+JHU         | EMNLP 2025          | 人机协作        |
| 12  | **Robin**              | FutureHouse     | Nature 2026         | 闭环湿实验       |
| 13  | **ERA**                | Google Research | Nature 2026         | 经验代码助手      |
| 14  | **Materials Lab MAS**  | —               | Commun. Mater. 2026 | 自主材料实验室     |
| 15  | **HTP Screening**      | ORNL (DOE)      | arXiv 2026          | 超算高通量筛选     |

---

## 一、搜索与探索（含多样性）

### 1.1 问题

LLM 首次输出通常不是最优解。如果只是「生成一个 idea → 跑实验」，质量上限就是单次采样的上限。搜索的本质是用计算换质量——多试几个方向，沿着有希望的方向深挖。

### 1.2 各系统做法

#### 1.2.1 线性采样 + 嵌入去重

**AI Scientist v1** 是最朴素的方式：high temperature (0.8) 生成 N 个 idea，每个独立跑实验，按结果排序。多样性由两个机制保证：(1) temperature 随机性；(2) **Novelty Filter**——用 Semantic Scholar 嵌入向量算余弦相似度，>0.85 标记为不新颖要求重生成。局限在于 idea 之间无信息共享——一个方向失败不影响其他方向的实验设计。

**Agent Laboratory** 依赖人类提供核心 idea，系统本身不做探索。人类写 notes 引导方向，Agent 只负责执行。

#### 1.2.2 MCTS 树搜索 (UCT/UCB)

**AI Scientist v2** 从「随机采样 N 个」升级为「有方向的树搜索」。四个步骤循环：

```
Selection:   从根沿 UCB 最大的路径走到叶子节点
             UCB(n) = value(n)/visits(n) + c * sqrt(log(N_parent) / n)
Expansion:   LLM 对叶子 idea 做「变异」——每次只改一个维度
Evaluation:  Pilot 实验（10% 数据 + 1/3 训练轮数 + 5min 超时）
Backprop:    奖励回传到根节点
```

**关键设计**：变异被显式约束为单一维度（改方法/改数据集/改假设/改消融），防止 LLM 一次改太多无法归因。UCB 的 exploration 项 `c * sqrt(log(N)/n)` 保证未访问的分支最终被探索。Pilot 实验以 1/10 的成本获取启发式信号，过滤掉 >80% 的低质量方向。

#### 1.2.3 锦标赛进化

**AI Co-Scientist** 用三个平行的 Generation Agent（基于文献/基于蛋白质结构/基于分子进化）从不同起点并行生成 30 个候选假设。然后辩论→淘汰→Top-K 生存者变异→生成新一代。变异不是随机的——LLM 定向修复 Critic 指出的最严重弱点。**本质是进化算法的 beam search，变异和交叉算子由 LLM 执行而非随机扰动**。

多样性由三个 Generator 的起点差异 + 锦标赛淘汰压力 + 交叉操作（合并两个假设的优点）共同保证。

#### 1.2.4 Contextual Bandit

**ATHENA** 将探索建模为 Contextual Bandit。Proposer 按 `P(A|H_n) ∝ exp(E_hat[R|H_n, A] / τ)` 选择期望奖励最大的动作。Coordinator 脑暴 3-5 个方向做多样性保证，Gatekeeper 过滤与历史失败过近的方向。**关键约束**：如果所有候选的预期奖励都低于历史最佳，Gatekeeper 返回空——宁可不变，也不冒险变差（submartingale 性质：`E[R_{n+1} | H_n] ≥ R_n`）。

**Re⁴** 不做主动搜索。Consultant 枚举 3+ 候选算法策略（如 FDM/FVM/Spectral），但枚举依赖 Consultant 的领域知识而非系统性搜索，没有反馈驱动的方向选择。

#### 1.2.5 知识图谱驱动的 Gap 分析

**DeepScientist** 不做随机搜索。先从 20+ 篇论文全文中构建知识图谱（节点：Paper/Method/Metric/Dataset，边：cites/improves_upon/evaluated_on），然后在图上做三类可计算查询：

- **饱和区**：论文密度高 + 指标标准差小 → 做透了，避开
- **SOTA 弱点**：最新方法在特定数据子集上的短板 → 这里有空间
- **未连接区**：方法 A 的理念可应用于问题 B 但没人做过 → 跨领域机会

**本质是把探索方向的选择从「LLM 的直觉」变成了图上的可计算查询**。加上 Intake Audit（独立 LLM 实例检查"有没有人做过类似的事"）作为第二道新颖性防线。

#### 1.2.6 Robin 的假设空间探索

**Robin** (Nature 2026) 的 Hypothesis Agent 在给定疾病靶点后，系统性地搜索已知药物空间中可能的候选分子。搜索空间由 (已知药物, 新靶点) 的笛卡尔积定义——药物重定位（drug repurposing）天然适合系统性搜索。

### 1.3 多样性保证手段汇总

| 手段                             | 使用系统            |
| ------------------------------ | --------------- |
| 高 temperature 采样               | AI Scientist v1 |
| 嵌入向量去重（>0.85 拒绝）               | AI Scientist v1 |
| 变异约束为单一维度                      | AI Scientist v2 |
| UCB exploration 项              | AI Scientist v2 |
| 多平行 Generator（不同数据源/方法论）       | Co-Scientist    |
| Coordinator 脑暴 + Gatekeeper 去重 | ATHENA          |
| 知识图谱 Gap 分析（可计算查询）             | DeepScientist   |
| Intake Audit 新颖性审查             | DeepScientist   |
| 药物空间笛卡尔积搜索                     | Robin           |

---

## 二、质量验证与筛选

### 2.1 问题

单一验证有盲区。程序化检查能发现数值越界但发现不了「实验设计本身有逻辑漏洞」。LLM Reviewer 能审代码质量但审不出隐蔽的数值 bug。两者的漏洞不同，必须联合使用。

### 2.2 六层防线（从浅到深）

#### L1：程序化语法/数值校验（~0.01s，无 LLM）

**EviBound** 的 POST-Gate 是典范——纯代码，不调 LLM，开销 ~0.01s：

- 产物完整性：文件存在？大小 > 0？
- Run 状态：MLflow status == FINISHED？
- 数值范围：每个 metric ∈ [physical_lower, physical_upper]？
- 不通过 → 直接 BLOCKED，不需要 LLM 判断

**ATHENA Inspector** 也在这一层——逐字段匹配「代码参数 == 策略要求」（层数、激活函数、BC 类型、损失权重），是结构化 diff 而非语义理解。

**ERA** (Nature 2026) 将这一层做到极致。科学家写自然语言 spec → 系统自动转化 spec 为 acceptance test suite（单元测试 + 集成测试 + 物理合理性测试）。代码必须通过所有测试才能被接受。**LLM 调用一次生成代码，测试套件近乎免费运行——零盲区（pass/fail 是二值的）**。

**HTP Screening** (ORNL) 在每个 DFT/MD 计算完成后自动运行后处理校验（能量收敛 < 1e-6 eV、力收敛 < 0.01 eV/A、几何合理性检查）。

**Materials Lab MAS** 的 Safety Agent 在湿实验操作前审查温度/压力/化学品兼容性，危险组合自动拒绝。

#### L2：实验设计逻辑校验（~0.5s，轻量 LLM）

**EviBound** 的 PRE-Gate：LLM 花 ~0.5s 检查验收标准 Schema 是否有效（"模型更好" → 拒绝，必须有量化标准）、声明是否可测（输出中有对应的 metric key？）、实验设计是否有逻辑漏洞（对照组对吗？变量控制对吗？）。

**Robin** 的 Protocol Validator Agent 在实验方案进入湿实验前独立审查：实验步骤是否逻辑合理、试剂用量是否在安全范围、对照组设置是否正确。不通过 → protocol 被退回重生成。

#### L3：独立角色审阅（LLM 深层调用，不同实例）

**Re⁴** 的核心发现：Reviewer 必须是**不同于 Programmer 的 LLM 实例**。审阅三个维度：(1) 代码质量——算法合理？离散化正确？数值稳定？(2) 结果质量——解物理合理？误差可接受？有 Gibbs 振荡？(3) 执行质量——有警告？收敛正常？关键输入不只是代码——还有运行时输出（stdout/stderr），避免纯代码审阅的局限性。

消融实验：Hilbert 系统 GPT-4.1-mini 0% → 57%（+Reviewer 后）。

**AI Scientist v1** 的 Reviewer Agent 用 NeurIPS 审稿标准审阅生成的论文，输出分数 + 修改建议。但 Paper Agent 和 Reviewer Agent 用的是同一个 LLM（仅不同 prompt）——这是 Re⁴ 明确反对的「同模型自审」。

#### L4：多维度辩论（多个独立 LLM，不同视角）

**AI Co-Scientist** 的 6 个 Critic Agent：毒性/合成可行性/药性/专利冲突/靶点特异性/代谢稳定性。每个有明确的领域检查清单。**本质是用多个窄域专家替代一个通用审阅者**——单一审阅者可能在某个维度上知识不够，6 个窄域专家各自聚焦。

**Sibyl H5**：Optimist（找证据支持假设）和 Skeptic（找证据推翻假设）的对立角色设计。Skeptic 的反对可以直接阻止声明或强制新验证任务。

**Materials Lab MAS**：Heartbeat 跨 Agent 异常检测——每个 Agent 定期发送 heartbeat（含当前状态、最近操作结果质量）。Agent A 发现 Agent B 的 heartbeat 显示"最近 XRD 表征结果噪声过高" → 触发 Alert → Orchestrator 自动重新安排表征。**跨 Agent 视角能发现单一 Agent 看不到的问题**。

#### L5：结构化证据验证（代码强制，不是 LLM 建议）

**ResearchLoop** 的 Claim Ledger：声明写入时必须绑定 Evidence Object（具体到文件的某行，如 `run_abc/metrics.json#L3`），代码自动提取实际数值与声明中数字做匹配。不匹配 → 直接 REJECTED，无需 LLM 判断。

**Sibyl H2**：5 级证据成熟度（执行完成 → 初步结果 → 分析就绪 → 发表就绪 → 已审计声明），每级有明确定义的可靠声明内容。声明不能超越证据级别——L1（代码跑通）只能声明「已执行」，L4（GCI < 5% + 统计检验 + 消融）才能声明「策略有效」。

**ERA** 的 acceptance test 体系同样在这一层——测试 pass/fail 是代码强制执行的，不是 LLM 的主观判断。

#### L6：真实世界验证（非 LLM，非代码）

**Robin** 的湿实验验证——假设最终送入机器人实验室，在真实物理世界中验证。AI 提出的候选分子在 AMD（致盲眼病）模型上确实有效。

**AI Co-Scientist** 的湿实验验证——提出的候选药物在体外实验中证实对 AML（急性髓性白血病）靶点有效。已部署到美国 17 个 DOE 国家实验室。

**Grounded Scrutiny** (arXiv 2026.04) 做的是反向验证：让 AI 复现已发表论文，发现相当比例的论文无法被 AI 复现——暴露出方法描述不完整、数值偏差、甚至数值伪影被当作真实效应。

### 2.3 消融实验揭示的「缺一不可」

| 配置                 | 幻觉率      | 说明                |
| ------------------ |:--------:| ----------------- |
| Prompt-only（无任何门控） | **100%** | LLM 看到模糊信号就声称成功   |
| 仅 POST-Gate        | **25%**  | 数值可以正常但声明可能是假的    |
| 仅 PRE-Gate         | 较高       | 拦得住声明漂移，拦不住代码 bug |
| PRE + POST 联合      | **0%**   | 两者漏洞不重叠           |

### 2.4 质量验证体系汇总

| 创新点                        | 来自                | 机制                      |
| -------------------------- | ----------------- | ----------------------- |
| POST-Gate 程序化数值校验          | EviBound          | 无 LLM，~0.01s，物理范围检查     |
| Acceptance Test 转化         | ERA               | spec → 自动生成测试 → 通过才接受代码 |
| Inspector 字段匹配             | ATHENA            | 代码参数逐字段对照策略要求           |
| 独立 Reviewer（不同 LLM 实例）     | Re⁴               | 防自审盲区，Hilbert 0%→57%    |
| Protocol Validator（湿实验前审查） | Robin             | 实验方案安全/逻辑/可行性独立审查       |
| 六维度 Debate（最完整）            | Co-Scientist      | 6 窄域专家替代单通用审阅者          |
| Heartbeat 跨 Agent 异常检测     | Materials Lab     | 不同视角互相监控，发现盲区问题         |
| 5 级证据成熟度                   | Sibyl H2          | 声明不能超越证据级别，代码强制         |
| Claim Ledger 证据绑定          | ResearchLoop      | 声明→证据链接，自动提取数值验证        |
| 后处理数值收敛校验                  | HTP Screening     | 能量/力/几何自动检查 + 连续失败降级    |
| 出版后复现验证                    | Grounded Scrutiny | AI 复现已发表论文，发现无法复现的      |
| Optimist/Skeptic 对立        | Sibyl H5          | 认知偏见互补，Skeptic 可否决声明    |
| Safety Agent（物理安全）         | Materials Lab     | 温压/化学品/兼容性自动审查          |

---

## 三、记忆管理

### 3.1 问题

AI 自主科研系统从「每次从零开始」到「跨系统知识进化」，记忆管理经历了四个代际的演进。核心问题是：存什么、存多久、谁可以用、怎么查询、能否驱动行为改变。

### 3.2 五级记忆架构

#### L0：无记忆

**Re⁴、EviBound、AI Scientist v1** 属于此类。Re⁴ 解完 Burgers 方程后，再遇到 Navier-Stokes 不会利用 Burgers 的经验。EviBound 的门控规则完全由人预设，不随经验改善。AI Scientist 每次运行是独立的——上一轮生成的论文信息不回传给下一次运行。

#### L1：会话内记忆

**ATHENA** 在单次 HENA 循环内维护完整历史：`H_n = {(A_0,S_0,O_0,R_0), ..., (A_{n-1},S_{n-1},O_{n-1},R_{n-1})}`。当轮 Proposer 分析全部历史做决策，Gatekeeper 用 RAG 检索相似历史失败过滤方向。但这只是**单次运行内**的记忆——关掉重来就全忘了。

**AI Scientist v1/v2** 用 git log 记录每轮的 diff、指标、分析。Paper Agent 写 Related Work 时引用前面 Phase 检索的论文。但同样限于单次运行。

**ERA** 的代码进化历史在单次运行内保留——每轮通过的代码候选 + 测试结果形成进化轨迹。

#### L2：持久化结构记忆

**DeepScientist** 的知识图谱是最系统化的持久记忆。将论文编码为图（Paper→Method→Metric→Dataset），支持：(1) 存储——每次 Scout 更新 KG；(2) 查询——Gap 分析 = 图查询，不是自由文本搜索；(3) 累积——随着论文增多，KG 越来越完整。**核心洞察：结构化图 > 非结构化文本集合**。

**ResearchLoop** 的 Evidence Object + Claim Ledger——每个实验结果被持久化为 `{run_id, metrics, artifacts, timestamp}`，每个声明写入 `{claim_text, evidence_refs, status, timestamp}`。**这是一种「研究级」的记忆——不是存论文摘要，而是存「这个实验证明了什么、证据在哪」**。

**AgentRxiv** (Agent Laboratory) 允许 Agent 将论文/代码/模型权重上传到公共平台，其他 Agent 可以检索、引用、在此基础上继续研究。模拟人类学术出版的累积式进步。

#### L3：角色路由记忆

**Sibyl H4**：核心论点——大多数系统把教训存在全局向量库里，但**教训是有受众的**。代码生成的教训应该只路由到 Programmer，质量评估的教训只路由到 Reviewer。不相关的 Agent 不应该被无关教训污染 prompt。

实现：每条教训带 `target_role` 字段。注入时按角色匹配过滤。例如：

```
"前缘 y+ 不达标，需减小 first_layer_height"
  → target_role: boundary_layer_agent（边界层策略 Agent）
  → 不注入：volume_mesh_agent（全局尺寸场 Agent）

"Jacobian 为负，体网格有翻转单元"
  → target_role: volume_mesh_agent
  → 不注入：boundary_condition_agent
```

**Robin** 同样有角色路由：Hypothesis Agent 接收文献相关的教训，Protocol Agent 接收实验步骤相关的教训，不交叉污染。

#### L4：系统级自演化记忆

**Sibyl H7**：重复失败不仅被记住，还**修改系统本身的运行规则**。Trial-to-Harness-Behavior：

```
连续 5 次 OOM       → Harness 新增「批量上限」校验门
连续 3 次 Gibbs 振荡 → Harness 新增「激活函数检查」gate
多次 lr 调优失败     → Harness 更新 lr 搜索策略
```

**GRAFT-ATHENA**：将成功的解法路径（如 `["KAN","strong_BC","PINN","Adam+LBFGS"]`）嵌入度量空间。新问题来了 → 查最近邻的成功路径 → 作为初始策略。**本质是把「这个组合在这类问题上有效」编码为可检索的向量**。从 1968 年 NASA 报告出发重建了阿波罗指令舱 Mach-10 流场。

**AI Co-Scientist**：验证有效的假设反馈回知识库，影响后续 round 的 Generation Agent。但粒度是「假设级」而非「方法级」。

### 3.3 记忆架构对比

| 级别      | 存什么                | 存多久  | 谁可用      | 查询方式    | 能否改行为 | 代表系统                        |
| ------- | ------------------ | ---- | -------- | ------- | ----- | --------------------------- |
| L0 无记忆  | —                  | —    | —        | —       | —     | Re⁴, EviBound               |
| L1 会话   | 实验历史+diff          | 单次运行 | 同一轮      | 顺序遍历    | ✗     | ATHENA, AI Scientist        |
| L2 持久结构 | KG/Ledger/Evidence | 持久文件 | 所有 Agent | 图查询/SQL | ✗     | DeepScientist, ResearchLoop |
| L3 角色路由 | 教训+target_role     | 持久文件 | 特定 Agent | 角色匹配    | ✗     | Sibyl H4, Robin             |
| L4 系统规则 | trigger→action→新规则 | 持久文件 | 系统本身     | 触发条件    | ✓     | Sibyl H7, GRAFT             |

---

## 四、实验和代码的管理与编排

### 4.1 问题

多个实验并行时，代码会迅速分叉——每个 idea 一个版本，每个调试迭代又一个版本。核心挑战：(1) 实验之间不互相污染；(2) 代码变更可追溯；(3) 好的修改保留、坏的丢弃；(4) 不同阶段（文献→实验→写作→审稿）的产物能按契约传递。

### 4.2 代码版本管理

#### AIDER 增量编辑（基于 git diff）

**AI Scientist v1/v2** 的核心实验引擎。每次实验迭代后，LLM 分析哪些改动有效、哪些无效 → 输出 unified diff 修改无效部分（而非全量重写）→ git log 天然提供完整的变更审计链。Token 消耗从 O(N) 降到 O(log N)，且保留有效代码不被全量重写破坏。

v2 进一步将版本管理扩展到 idea 树——每个节点有独立的代码快照 + pilot 结果。树的路径即是「这个 idea 是怎么从根 idea 一步步变异过来的」。

#### Git Worktree 物理隔离

**DeepScientist** 有完整的 GitOps 系统。每个 Quest（研究任务）创建独立的 git worktree：

- **物理隔离**：不同 Quest 在不同 worktree，互不污染
- **并行友好**：多个 Quest 同时在不同 worktree 中执行
- **分支管理**：每个 Quest = 一个 git branch，实验结果 commit 到对应分支
- **可回滚**：scout → idea → baseline → deep research，每个阶段一个 commit。失败可回滚到任意阶段

#### Cell-by-Cell 分解 + 接口契约

**ATHENA** 将代码生成分解为 7 个代码单元（Cell），每个 Cell 有明确的输入/输出契约。出错时只重生成出问题的 Cell，不影响其他。**本质是用「模块化 + 接口契约」降低单次 LLM 调用的复杂度**。Planner → Inspector → Debugger 的流水线保证每个 Cell 是忠实且可执行的。

#### ERA 的进化式代码生成

**ERA** (Nature 2026) 每次迭代生成多个代码候选 → 对所有候选运行 acceptance test suite → 只有通过的候选进入下一轮 → 通过候选之间再做性能比较（运行时间、内存占用）。**本质是结合了测试驱动开发 + 进化算法的代码生成**。

### 4.3 实验编排

#### 阶段化瀑布（顺序流水线）

**Agent Laboratory**：PhD（文献）→ Postdoc（计划）→ ML Engineer（数据准备）→ ML+SW Engineer（实验执行）→ Postdoc（结果分析）→ Professor（写作）→ Reviewers（审稿）。每个阶段的输出是下一阶段的输入，阶段之间有明确的产物契约（如 lit review 输出必须是 .md + .bib，实验输出必须是 metrics.json + plots）。

**Re⁴**：Rewriting → Resolution → Review → Revision。也是顺序流水线，但 Revision 阶段有内循环（Programmer ↔ Reviewer 迭代 2-4 轮）。

#### 状态机编排

**ResearchLoop**：6 个状态（IDLE → PLANNING → RUNNING ↔ REPAIRING → OBSERVING → ANALYZING → CLAIMING → CLOSED），只有合法转换被允许。非法转换（如 PLANNING → CLAIMING 跳过实验，或 RUNNING → CLOSED 跳过证据收集）被代码拒绝。

**Sibyl**：19 阶段状态机 + 外循环（跨 8 个 issue 类别监控）。内循环做研究迭代，外循环检测重复失败模式 → 修改内循环的 Harness 配置。

**DeepScientist**：Quest 生命周期状态机——Scout → Idea → Baseline → DeepResearch → Paper → Done。每个状态的前置条件被代码强制（如进入 Idea 必须有 `knowledge_graph.json`）。

#### Supervisor-Worker 编排（并行+中心调度）

**Robin** (Nature 2026)：Orchestrator Agent 是中心调度器，管理 Hypothesis / Protocol / Validator / Robotics / Data / Analysis / Report 7 个 Agent 的并行工作。关键是**流水线并行**——Hypothesis Agent 生成假设时，Data Agent 可以同时处理上一轮的数据。

**Co-Scientist**：Supervisor 协调多个 Generation Agent + 多个 Critic Agent + Meta-Reviewer。Supervisor 不自己生成假设，而是分配任务、收集结果、做最终 Meta-Review。**策略层（Supervisor）和执行层（Workers）分离**。

**Materials Lab MAS**：实验队列 + 资源锁机制。当一个实验在合成材料时（需要 2 小时），Agent 自动将另一个实验的表征任务分配到空闲的表征设备。**本质是 Job Shop Scheduling 问题，用 LLM 做调度器**。

**HTP Screening** (ORNL)：Supervisor Agent 维护全局任务 DAG——无依赖的任务全部并行提交到 Slurm。任务完成 → 自动触发下游任务。任务失败 → 自动重试（最多 3 次）→ 仍失败 → 标记 + 人工通知。**1000+ 任务并发**的超算级编排。

#### 实验隔离

所有系统都用 **Docker 沙箱**（AI Scientist、ATHENA、DeepScientist）或 **conda venv**（Agent Laboratory）做执行隔离。ATHENA 的 Docker 预装 PyTorch/JAX/DeepXDE/FEniCS/Gmsh，资源限制 32GB 内存 + 24GB VRAM + 1 小时超时。

Robin 的隔离扩展到**物理世界**——机器人实验室有安全锁、温度监控、操作超时。

### 4.4 编排模式对比

| 模式                | 代表系统                               | 并行度           | 适用场景       |
| ----------------- | ---------------------------------- |:-------------:| ---------- |
| 顺序瀑布              | Agent Lab, Re⁴                     | 1             | 单实验串行      |
| 状态机               | ResearchLoop, Sibyl, DeepScientist | 1             | 需严格流程控制    |
| AIDER git diff    | AI Scientist v1/v2                 | 1 实验内 N 迭代    | 实验内代码迭代    |
| Cell-by-Cell      | ATHENA                             | 7 Cells 可部分并行 | 代码生成       |
| Git Worktree      | DeepScientist                      | 多 Quest 并行    | 多研究任务隔离    |
| Supervisor-Worker | Robin, Co-Scientist                | 10+ Agent 并行  | 多 Agent 协作 |
| Job Shop Queue    | Materials Lab                      | 多设备并行         | 物理实验室      |
| HPC DAG + Slurm   | HTP Screening                      | **1000+ 并发**  | 超算         |

---

## 五、计算效率和并行实验

### 5.1 问题

AI 自主科研系统的成本大头在 LLM 调用（$0.01-$1/次）和 GPU 时间（~$1-10/小时）。如果让 Agent 无策略地串行跑所有实验，耗时和成本会让系统不可用。效率优化的核心：**用廉价信号替代昂贵计算，用并行填充空闲时间**。

### 5.2 效率优化策略

#### 策略 1：用测试替代 LLM 审阅

**ERA** 的核心效率创新。LLM 审阅代码质量 = $0.01-0.1/次，有盲区。测试套件运行 = 近乎免费，零盲区。1 次 LLM 代码生成 → 自动跑所有 acceptance test → 只有通过的代码才花 LLM token 做深度分析。相比纯 LLM 驱动的审阅（如 Re⁴ Reviewer），**节省约 60-80% 的 LLM 调用**。

**EviBound** 的 POST-Gate 同理——程序化数值校验 ~0.01s（无 LLM），拦截 25% 的幻觉，开销仅 8.3%。

#### 策略 2：Pilot → Full 分级实验

**AI Scientist v2** 的核心效率创新：

```
完整实验: 100% 数据 + 100% 训练轮数 + 完整评估 → $X
Pilot 实验: 10% 数据 + 33% 训练轮数 + 核心指标   → $0.1X

Pilot 用于:
  - 树搜索中快速评估节点 (UCB value)
  - 过滤掉明显没希望的方向 (>80%)
  - 仅对 top 2-3 路径运行完整实验
```

**Co-Scientist** 同理——Debate 阶段的 Critic 评估是纯 LLM 调用（无实验），只有 Meta-Review 批准的 Top-3 假设才送入实际实验验证。

#### 策略 3：Wet Lab First（计算便宜，多验证；实验贵，少犯错）

**Robin** 的成本结构天然驱动最优策略：LLM 调用 ~$0.01-1，湿实验 $100-1000/实验。因此系统在计算层做充分的验证和筛选（多轮 Hypothesis+Protocol+Validator），只让最有信心的 1-2 个方案进入湿实验。

#### 策略 4：流水线并行（压缩空闲时间到零）

**Robin**：机器人实验室支持多孔板并行。Orchestrator 在等待一个实验的孵育时间时，启动另一个实验。**本质是将机器人实验室的空闲时间压缩到零**。效果：**2 小时完成人类科学家数月的工作量——约 200 倍加速**。

**Materials Lab MAS**：多个实验设备（合成、表征、测试）可并行运行。Agent 维护实验队列 + 资源锁。当设备 A 正在合成材料（需要 2h），自动将另一个实验的表征任务分配到空闲的表征设备 B。

#### 策略 5：超算级任务并行

**HTP Screening** (ORNL) 在 Frontier 超算上同时管理 1000+ 材料计算任务。无依赖的任务全部并行提交到 Slurm。Supervisor 实时监控 Slurm 队列状态——任务完成 → 自动触发下游任务。**这是目前规模最大的并行科学实验 Agent 系统**。

#### 策略 6：多 Idea 独立并行

**AI Scientist v1**：`launch_scientist.py` 支持并行生成 N 个 idea、各自独立跑实验。简单的进程级并行，每个 idea 在独立的 Docker 沙箱中运行。

**Co-Scientist**：3 个 Generation Agent 并行从不同角度生成假设。6 个 Critic 可并行评估不同假设。

### 5.3 效率数据汇总

| 系统               | 速度/效率                 | 机制                  |
| ---------------- | --------------------- | ------------------- |
| Robin            | **200x 加速**（2h vs 数月） | 流水线并行 + 多孔板并行       |
| ERA              | LLM 调用减少 60-80%       | 测试替代 LLM 审阅         |
| EviBound         | 开销仅 8.3%              | POST-Gate 纯程序化      |
| AI Scientist v2  | >80% 方向被 Pilot 过滤     | Pilot 实验分级          |
| HTP Screening    | **1000+ 并发**          | Slurm + HPC DAG     |
| Materials Lab    | 24/7 无人值守             | Job Shop Scheduling |
| AI Scientist v1  | $15/篇                 | 模板 + AIDER 增量       |
| Agent Laboratory | **$2.33/篇**           | DeepSeek V3 + 简单流水线 |
| DeepScientist    | 2 周 vs 人类 3 年         | KG 驱动 + 模块化替换       |

---

## 六、计算预算管理

### 6.1 问题

LLM 调用 = $。GPU 时间 = $。如果让 Agent 无限制地跑，成本可能指数级增长。如何在固定预算内获得最大回报？五种递进策略。

### 6.2 各系统做法

#### 策略 1：硬限制（最朴素，最常用，全部系统）

```
AI Scientist v1/v2:   max_iterations=5, timeout=3600s, max_ideas=N
Agent Laboratory:     max_steps=100, mlesolver_max_steps=3, papersolver_max_steps=5
ATHENA:               max_hena_rounds=10, debugger.max_rounds=5, inspector max_retries=2
DeepScientist:        Quest depth=20 (scout 论文数), max deep research rounds
```

每个系统都有这些参数。**硬限制是安全网，防止 runaway 消耗**。

#### 策略 2：早期停止（有信号驱动的智能终止）

```
AI Scientist v1:   实验结果满足 hypothesis → 立即停止迭代
AI Scientist v2:   所有子节点 UCB < 父节点 → 不再深挖此路径
                   （MCTS 的收敛性质作为自然终止条件）
ATHENA:            early_stop_patience=3 (连续 3 轮奖励无改进)
                   improvement_threshold=0.01
                   Gatekeeper 返回空（无安全新方向）→ 整轮跳过
ERA:               连续 N 轮无候选通过 acceptance test → 
                   自动暂停，要求人类澄清 spec
```

**核心思想**：不是盲目的「跑满 N 轮」，而是「有进展才继续」。

#### 策略 3：Tournament 淘汰（质量驱动的自适应预算分配）

**AI Co-Scientist** 是最典型的代表。30 个候选 → 6 个 Critic 打分 → Top-5 生存 → 变异 → 下一轮。**质量差的假设自然被淘汰，不消耗后续轮次的 LLM 调用**。最终只有 Top-3 进入 Meta-Review。本质是「好的假设获得更多 compute，差的假设被早期淘汰」。

**Robin** 同理——计算层做充分验证（便宜），只有最有信心的 1-2 个方案进入湿实验（昂贵）。

#### 策略 4：自适应资源分配（失败越多→预算越紧；成功越多→预算越多）

**Sibyl H6** 是唯一显式讨论此主题的论文。浪费的试验重塑预算分配和健全性检查排序：

```
连续 3 次 OOM       → 自动降低 batch size / 网格精度
连续收敛失败         → 增加 lr 搜索预算但降低模型规模
高质量结果           → 增加该方向的实验预算
低质量结果           → 减少该方向资源，资源转给其他方向
```

**HTP Screening** 同样有优先级调度——高优先级材料（如能源材料）获得更多超算 node hours，低优先级任务在资源紧张时自动排队。**用 LLM 做优先级决策**。

#### 策略 5：测试失败 = 预算信号（不烧 token 在死路上）

**ERA** 的隐式预算控制机制：acceptance test 通过率是信号。如果连续 N 轮没有候选通过测试 → 系统自动调整——不是增加 LLM 调用，而是要求人类科学家澄清/修改 spec。**本质是用测试失败作为「这个方向可能有问题」的早期信号**，避免在错误方向上燃烧 token。

#### 策略 6：Scaling Law 驱动的模型选择

**AI Scientist** Nature 论文发现了 R²=0.517 的 Scaling Law——更强的模型 → 更好的论文质量。这意味着预算管理不仅是「省钱」，还是「在给定预算下选最强模型 vs 多跑几个弱模型实验」的 trade-off。论文建议：固定预算下，用最强模型跑少量高质量实验 > 用弱模型跑大量低质量实验。

### 6.3 预算管理策略对比

| 策略              | 代表系统                      | 核心机制                                   |
| --------------- | ------------------------- | -------------------------------------- |
| 硬限制             | 全部 15 个系统                 | max_iterations/timeout/max_rounds（安全网） |
| 早停（信号驱动）        | ATHENA, AI Scientist, ERA | patience, UCB 收敛, hypothesis 满足        |
| Tournament 淘汰   | Co-Scientist, Robin       | 质量差→淘汰，好→更多 compute                    |
| 自适应分配           | Sibyl H6, HTP Screening   | 失败→减预算，成功→加预算                          |
| 测试失败 = 停止信号     | ERA                       | 连续无候选通过 → 要求澄清 spec                    |
| Scaling Law 选模型 | AI Scientist (Nature)     | 强模型少实验 > 弱模型多实验                        |
| Wet Lab First   | Robin                     | 计算便宜多验证 → 实验贵少犯错                       |

### 6.4 统一预算管理架构

综合 15 个系统的最优实践，可以提炼一个五层预算管理模型：

```
L1: 硬限制（全局安全网）
    总 LLM 调用上限 / 总 GPU 时间上限 / 墙钟超时
    来自: 全部系统

L2: 每阶段配额（防止任一阶段消耗不成比例）
    Scout 50 次 LLM / Experiment 100 次 LLM + 20 GPU hours / Paper 30 次 LLM
    来自: Agent Laboratory, DeepScientist

L3: Pilot 加速（廉价信号替代昂贵计算）
    不重要的方向 → Pilot ($0.1X)；有把握的 → Full ($X)
    来自: AI Scientist v2, Co-Scientist

L4: 自适应调整（从失败中动态调整）
    连续 3 次 OOM → 自动减 batch；有希望方向 → 加预算
    来自: SIBYL H6, HTP Screening

L5: 早停（有进展才继续）
    连续 3 轮无改进 → 停止；所有 UCB < 父节点 → 停止
    来自: ATHENA, AI Scientist v2
```

---

## 附录：六维度 × 十五系统速查矩阵

| 系统                    | 搜索与探索                          | 质量验证                       | 记忆管理              | 代码编排                      | 并行效率                       | 预算管理                     |
| --------------------- |:------------------------------:|:--------------------------:|:-----------------:|:-------------------------:|:--------------------------:|:------------------------:|
| **Re⁴**               | Consultant 枚举                  | 独立 Reviewer                | L0 无记忆            | 顺序 4 阶段                   | —                          | 固定 N 轮 Revision          |
| **ATHENA**            | Contextual Bandit + Gatekeeper | Inspector + VLM + Critic   | L1 会话历史           | Cell-by-Cell              | —                          | patience + Gatekeeper 终止 |
| **GRAFT-ATHENA**      | Fingerprint 最近邻                | —                          | L4 Fingerprint 迁移 | —                         | —                          | —                        |
| **Sibyl**             | H5 Optimist/Skeptic 博弈         | H2 5 级成熟度 + H5 角色分离        | L3 路由 + L4 自演化    | 19 阶段状态机                  | —                          | H6 自适应分配                 |
| **EviBound**          | —                              | PRE+POST 双门控               | L0 无记忆            | 无关（门控层）                   | —                          | 开销仅 8.3%                 |
| **ResearchLoop**      | —                              | Claim Ledger 证据绑定          | L2 持久 Ledger      | 6 状态机                     | —                          | 固定转换规则                   |
| **AI Scientist v1**   | 线性采样 + 嵌入去重                    | Reviewer（同模型自审）            | L1 git log        | AIDER git diff            | N idea 并行                  | max_iter + timeout       |
| **AI Scientist v2**   | MCTS + UCB + Pilot             | Pilot 实验信号                 | L1 树节点历史          | AIDER + 树节点快照             | —                          | Pilot 过滤 + Scaling Law   |
| **DeepScientist**     | KG Gap 分析 + Intake Audit       | 统计显著性检验 + Verify Agent     | L2 知识图谱           | Git Worktree + Quest 状态机  | —                          | depth=20 + 固定轮数          |
| **Co-Scientist**      | 锦标赛进化 + 3 Generator            | 6 Critic Debate + 湿实验      | L4 知识库更新          | Supervisor-Worker         | 3 Generator + 6 Critic 并行  | Tournament 淘汰            |
| **Agent Laboratory**  | —（人类提供 idea）                   | Reviewer（同模型）              | AgentRxiv         | 阶段化瀑布                     | —                          | max_steps 硬限制            |
| **Robin**             | 药物空间笛卡尔积搜索                     | Protocol Validator + 湿实验   | L3 角色路由           | Orchestrator + 7 Agent 并行 | 多孔板 + 孵育时间填充 → **200x 加速** | Wet Lab First            |
| **ERA**               | 进化式代码生成                        | Acceptance Test 5 层金字塔     | L1 进化轨迹           | 测试驱动的进化生成                 | —                          | 测试失败→停止信号                |
| **Materials Lab**     | —                              | Heartbeat 跨 Agent + Safety | —                 | Job Shop Queue + 资源锁      | 多设备 24/7 并行                | —                        |
| **HTP Screening**     | —                              | 后处理数值收敛校验                  | —                 | Slurm DAG + Supervisor    | **1000+ 并发**               | HPC 优先级调度                |
| **Grounded Scrutiny** | —                              | 复现已发表论文→发现错误               | —                 | —                         | —                          | —                        |

---

## 七、各系统缺点汇总

### 7.1 Re⁴

| #   | 缺点                          | 说明                                                                                                                      |
| --- | --------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| 1   | **Reviewer 本身也是 LLM，有审查盲区** | 如果 Reviewer 的领域知识不足以鉴别错误（如不知道某种数值格式在特定流动状态下会发散），会给出错误的"通过"意见。Hilbert 系统上即使加 Reviewer 最高也只有 57% 成功率——Reviewer 的数学能力就是天花板 |
| 2   | **无执行隔离**                   | Programmer 直接在宿主环境执行代码，没有沙箱保护。`os.system("rm -rf /")` 如果在生成代码中出现后果严重                                                    |
| 3   | **上下文截断**                   | 长输出（如迭代时步错误日志）被截断以避免超出 token 限制。Reviewer 可能看不到完整的错误信息，影响诊断准确性                                                           |
| 4   | **Reviewer 反馈质量不可控**        | 反馈是自由文本而非结构化输出，Programmer 可能误解或忽略 Reviewer 的建议。没有机制强制 Programmer 采纳每一条具体建议                                              |
| 5   | **角色数量固定**                  | Consultant-Programmer-Reviewer 三角色是硬编码的，不能根据问题复杂度动态增减角色                                                                 |
| 6   | **无跨问题学习**                  | 每次实验从零开始。解决完 Burgers 方程后，再遇到 Navier-Stokes 不会利用 Burgers 的经验                                                             |
| 7   | **无记忆系统**                   | 没有任何形式的持久化记忆或教训积累机制                                                                                                     |

### 7.2 ATHENA

| #   | 缺点                                            | 说明                                                                                                 |
| --- | --------------------------------------------- | -------------------------------------------------------------------------------------------------- |
| 1   | **复杂度极高**                                     | 完整实施需要 ~10 个 Agent 角色、5 个知识蓝图、cell-by-cell 重构协议、多模态观测处理。这不是一个周末能搭建的系统                              |
| 2   | **Conceptual Scaffolding 是瓶颈**                | 效果高度依赖 5 个专家蓝图的质量。如果蓝图有遗漏（如没覆盖某类 PDE 的最优数值方法），系统会系统性地在该类问题上失败。本质上是"专家知识的编码质量决定 Agent 上限"           |
| 3   | **视觉感知不可靠**                                   | 让 LLM"看图判断有没有 Gibbs 振荡"依赖 VLM 的视觉理解能力。对于专业科学可视化图像（流场伪彩图、网格质量云图），通用 VLM 的理解能力有限                     |
| 4   | **每轮成本高**                                     | 完整一次 HENA 循环涉及 5+ LLM 调用，包括 Strategy 的历史分析（长上下文）、Inspector 的代码审阅（长上下文）、Advisor 的多模态分析。论文没有公布详细成本数据 |
| 5   | **Cell-by-cell 重构细节不透明**                      | 论文描述了"逐单元重构"协议但实现细节不够透明，难以直接参考或复现                                                                  |
| 6   | **GRAFT 的 Fingerprint Embedding 对局部问题可能不够精细** | 网格策略的差异往往体现在局部（如前缘加密方式），而非全局。嵌入可能丢失局部细节                                                            |
| 7   | **Submartingale 保证依赖 Critic 判断质量**            | 如果 Critic 高估了一个坏策略，奖励会下降——保证不是确定性的，只是"在 Critic 足够好的前提下"                                            |

### 7.3 GRAFT-ATHENA

| #   | 缺点                              | 说明                                                     |
| --- | ------------------------------- | ------------------------------------------------------ |
| 1   | **Fingerprint Embedding 的粒度限制** | 解法路径嵌入基于全局策略选择（如架构+数值方法），对于"同一方法但局部参数不同"的细微差异可能区分不够    |
| 2   | **冷启动问题**                       | 需要足够的成功路径来训练 Fingerprint Embedding。新领域没有历史成功案例时退化为随机猜测 |
| 3   | **未独立发表**                       | 作为 ATHENA 的扩展发表，缺乏独立的消融实验和基准对比                         |
| 4   | **跨领域的泛化未验证**                   | 只在 SciML 领域验证。是否能迁移到湿实验、材料科学等领域未知                      |

### 7.4 Sibyl-AutoResearch

| #   | 缺点                                       | 说明                                                                                         |
| --- | ---------------------------------------- | ------------------------------------------------------------------------------------------ |
| 1   | **20+ Agent，过度复杂**                       | 19 阶段状态机的维护成本极高。论文中的实验规模也不大（主要是展示概念），是否能扩展到大规模多领域研究未验证                                     |
| 2   | **Skeptic 角色的"反对权"强度未明确定义**              | 如果 Skeptic 可以一票否决任何声明，系统可能过于保守永远不发表。如果 Skeptic 的反对太弱，就没有实际约束力。这种平衡是启发式的，缺乏理论保证             |
| 3   | **Routed Memory 怎样决定路由到哪个 Agent 缺乏清晰算法** | 论文描述了"教训按角色路由"的概念但实现细节不足。如何准确判断一条教训的受众？路由错误会污染无关 Agent 的上下文                                |
| 4   | **代码未开放核心部分**                            | 虽然仓库存在，但主要是 Claude Code 的配置文件——核心的 Harness 逻辑可能在封闭的商业环境中，不可复现                              |
| 5   | **7 条原则是 heuristic 非理论**                 | 与 ATHENA 的 Contextual Bandit 数学形式化或 ResearchLoop 的状态机形式化不同，Sibyl 的 H1-H7 是经验性原则，缺乏严格的正确性证明 |
| 6   | **外循环的自演化可能过度拟合**                        | 外循环基于历史失败模式修改 Harness，但可能产生"对过去问题过度优化、对未来问题准备不足"的过拟合                                       |

### 7.5 EviBound

| #   | 缺点                          | 说明                                                                                                            |
| --- | --------------------------- | ------------------------------------------------------------------------------------------------------------- |
| 1   | **门控规则需要人定义**               | 哪些 Schema 算"有效"、哪些产物算"必需"、哪些数值范围算"合理"——这些是领域知识，EviBound 不帮你自动生成。你需要自己写"first_layer_height ∈ [1e-7, 1e-3]"这种规则 |
| 2   | **粗粒度——拦得住幻觉，拦不住微妙错误**      | 门控是粗粒度的——能拦住明显幻觉，拦不住"结果大体正确但收敛不够好"的微妙问题                                                                       |
| 3   | **PRE-Gate 的 LLM 逻辑检查能力有限** | LLM 做实验设计逻辑漏洞检测时，对复杂的物理问题可能漏掉逻辑漏洞                                                                             |
| 4   | **不进化**                     | 门控规则不随经验改善。如果门控规则本身有问题（如物理范围太宽导致虚假通过），系统不会自己修正                                                                |
| 5   | **不验证物理合理性**                | POST-Gate 只检查数值范围，不检查"这个解在物理上是否合理"。一个满足数值范围但完全违反物理规律的解可以通过门控                                                  |
| 6   | **仅覆盖声明和结果校验，不涉及代码逻辑**      | 代码中的隐蔽 bug（如错误的评估指标、数据泄露）既不被 PRE-Gate 也不被 POST-Gate 检查                                                        |

### 7.6 ResearchLoop

| #   | 缺点                              | 说明                                                                                                       |
| --- | ------------------------------- | -------------------------------------------------------------------------------------------------------- |
| 1   | **协议规范相对抽象**                    | 状态模型和转换规则描述是概念级的，实现细节对非协议设计背景的开发者不直观                                                                     |
| 2   | **实验规模验证有限**                    | 9 次迭代验证了设计但实验规模较小，是否能在大规模、多领域科研中保持有效性未验证                                                                 |
| 3   | **缺少类似 EviBound 的清晰消融实验**       | 没有"有状态机 vs 无状态机"的对比数据。状态机的附加值没有量化                                                                        |
| 4   | **状态机的刚性 vs 科研的柔性**             | 科研过程本身是高度非结构化的——灵感可能在任何阶段出现。严格的 6 态机可能阻碍创造性的跳跃。你在 ANALYZING 阶段突然想到一个新假设——状态机允许你回到 PLANNING 还是必须先 CLOSED？ |
| 5   | **Claim Ledger 的自动验证依赖精确的数值解析** | `_extract_value("run_001/metrics.json#L3-L7")` 要求产物格式标准化。如果 Agent 生成的 JSON 格式略有偏差，数值提取会失败，导致合法声明被误拒绝     |
| 6   | **生态系统不如其他系统成熟**                | 没有开源社区，没有第三方集成，没有多领域适配案例                                                                                 |

### 7.7 AI Scientist v1

| #   | 缺点                            | 说明                                                                                                        |
| --- | ----------------------------- | --------------------------------------------------------------------------------------------------------- |
| 1   | **模板依赖 = 领域锁定**               | 系统只能研究"模板支持的那类问题"。如果要研究一个新领域（如数值方法、材料科学），需要人类先写好模板。模板质量直接决定研究质量的上限                                        |
| 2   | **可重复性危机**                    | 论文报告 AI Scientist 生成了数十篇论文，但只有少数几篇被人工验证可以重现。很多生成的论文存在实验错误（如过拟合到测试集、错误的基线比较），这些问题 Reviewer Agent 未必能发现     |
| 3   | **Self-Review 盲区**            | Reviewer Agent 和 Paper Agent 用的是**同一个 LLM**（仅不同 prompt）。这与 Re⁴ 和 ATHENA 的"独立 Reviewer"设计不同——同模型审同模型有结构性盲区 |
| 4   | **实验不可靠**                     | 生成的代码可能包含隐蔽的 bug（如错误的评估指标、数据泄露），而这些 bug 产生的"好结果"可能被 LLM 误认为是科学突破。EviBound 的消融实验（100% 幻觉率无门控）说明这类风险真实存在    |
| 5   | **文献检索粗放**                    | 仅用 Semantic Scholar 关键词搜索 + 摘要，不读取全文。文献综述可能遗漏关键相关工作，导致"新颖性"判断不准                                           |
| 6   | **Idea Generation 是一次性的、线性的** | 生成 N 个 idea，各跑实验，选最好。idea 之间无信息共享——方向 A 失败了不会影响方向 B 的实验设计。缺乏迭代式改进                                         |
| 7   | **无跨实验学习**                    | 每次运行独立，上一次生成的论文不回传给下一次运行                                                                                  |
| 8   | **成本无法控制**                    | 除了 max_iterations 和时间超时，没有精细的预算管理或自适应资源分配                                                                 |

### 7.8 AI Scientist v2

| #   | 缺点                            | 说明                                                                                   |
| --- | ----------------------------- | ------------------------------------------------------------------------------------ |
| 1   | **Pilot 信号可能误导**              | 小规模 pilot 实验的结果不一定能推广到完整实验（领域偏移问题）。一个好的 pilot 结果可能只是过拟合了小数据集，导致在错误的方向上深挖             |
| 2   | **变异空间受 LLM 想象力限制**           | 即使有树搜索，"变异"也是由 LLM 生成的。如果 LLM 在某个领域知识不足，变异会困在局部最优                                    |
| 3   | **成本 3× vs v1**               | 每个 pilot 实验也是一次 LLM 调用 + 代码执行。如果预算有限，v1 可能更经济。论文没有给出"$45 是否值 3× 质量提升"的 clear ROI     |
| 4   | **树搜索的开销未被充分讨论**              | MCTS 的 Selection 步骤需要维护和更新大量节点的 UCB 值。当树深度达到 3 层、每层 3 个子节点时，单次搜索需要 ~10 次 LLM 调用只用于评估 |
| 5   | **继承了 v1 的所有结构性问题**           | 模板依赖、Self-Review 盲区、文献检索粗放——这些 v1 的问题在 v2 中并未被解决。v2 只改进了 Idea Generation             |
| 6   | **Pilot 和 Full 之间的 gap 未被量化** | Pilot 的 10% 数据 + 1/3 轮数能在多大程度上预测 Full 实验的结果？如果 gap 很大，树搜索会系统性地错误选择方向                 |

### 7.9 DeepScientist

| #   | 缺点                         | 说明                                                                                                                                       |
| --- | -------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **Deep Research 成本极高**     | 全文阅读 20+ 篇论文 + 构建知识图谱，单是 Scout 阶段就需要巨额 LLM token 消耗。假设每篇论文 10K tokens，20 篇就是 200K tokens（仅阅读，不算分析和图谱构建）。加上 KG 构建、分析、查询，总成本可能在 $50-100+/篇 |
| 2   | **领域固定在 NLP/ML**           | 系统设计依赖 benchmark + 标准化评估。对于 CFD/材料科学等"非标准化评估"的领域，Scout 阶段的指标比较和 gap 识别机制不适用                                                              |
| 3   | **知识图谱维护成本高**              | KG 会过时。如果不持续更新，gap 分析会滞后于最新发表。但持续更新又推高了运行成本                                                                                              |
| 4   | **Idea 的创造性仍然有限**          | 虽然能超越 SOTA，但改进方式主要是"将已知方法 A 应用到新问题 B"或"在 SOTA 的弱点上修补"，缺少范式级的创新（如 Transformer 替代 RNN 这种颠覆性想法）                                             |
| 5   | **KG 构建质量依赖 LLM 的信息提取准确度** | 从论文全文中提取结构化信息（方法名、指标值、关系）依赖 LLM 的 NLP 能力。提取错误 → KG 有噪声 → Gap 分析偏离                                                                        |
| 6   | **未正式发表（截至 2026.07）**      | 仍为 arXiv 预印本，尚未通过正式同行评审                                                                                                                  |

### 7.10 AI Co-Scientist

| #   | 缺点                              | 说明                                                                                                                 |
| --- | ------------------------------- | ------------------------------------------------------------------------------------------------------------------ |
| 1   | **不包含实验执行**                     | Co-Scientist 只到"提出实验方案"，不包含自主执行实验。它依赖人类科学家来验证。这与 AI Scientist 的全自动定位互补，但对于追求端到端自动化的场景是一个根本性限制                      |
| 2   | **领域专业性极强——Critic 检查清单需人类专家构建** | 什么是 Lipinski 五规则？肝毒性信号有哪些？——这些是人工编码的领域知识。本质上是 ATHENA 的 Conceptual Scaffolding 在药物领域的对应物。如果换一个新领域，Critic 团队需要完全重新构建 |
| 3   | **成本 + 延迟**                     | 30 个候选 × 6 个 Critic × 5 轮进化 = 900+ 次 LLM 调用（每轮）。如果是 GPT-4 级别模型，纯假设生成阶段就可能花费数千美元                                    |
| 4   | **Hypothesis 的创造性边界**           | 即使有进化，变异的范围受限于 LLM 的知识。对于完全未知的机制（如"这个靶点的功能本身还不清楚"），系统效果未经验证                                                        |
| 5   | **未开源**                         | Google 没有开源 Co-Scientist 代码。非官方复现 (Kaimen-Inc) 覆盖面有限。这使得独立验证和社区改进变得困难                                              |
| 6   | **Tournament 的评估偏差**            | 所有 Critic 都是 LLM——如果多个 Critic 共享相同的知识盲区（如都不知道某个最新的毒性机制），会把一个事实上危险的分子评为高安全性                                         |
| 7   | **人类在循环中的位置不明确**                | 论文强调 human-in-the-loop，但人类的介入时机、决策权限、反馈如何融入系统——这些细节不透明                                                             |

### 7.11 Agent Laboratory

| #   | 缺点                             | 说明                                                                                                                                      |
| --- | ------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **依赖人类提供核心 idea**              | 系统的价值上限受限于人类的初始 idea 质量。与 AI Scientist 的自主 idea generation 互补但不替代。如果人类给的 idea 不好，整个 pipeline 的输出也不会好                                    |
| 2   | **缺乏深度文献理解**                   | PhD Agent 只读摘要（非全文），可能漏掉关键细节。这与 DeepScientist 的全文阅读 + 知识图谱形成鲜明对比                                                                        |
| 3   | **实验执行的鲁棒性有限**                 | MLESolver 的"生成→执行→修复"循环不如 AI Scientist 的 AIDER 增量编辑精细。复杂 bug 可能导致整个实验链失败，且没有错误分类和定向修复策略（不像 ATHENA Debugger）                             |
| 4   | **角色之间的信息传递是单向的**              | PhD→Postdoc→Professor 瀑布式。没有多角色之间的辩论或交叉验证（不像 Co-Scientist 的 Debate 机制或 Sibyl 的 Optimist/Skeptic 对立）。Postdoc 发现 PhD 的文献综述有遗漏 → 没有机制能回溯修正 |
| 5   | **代码质量参差不齐**                   | ML/SW Engineer 生成的代码可能有隐蔽 bug，没有 ATHENA 的 Inspector 来验证"代码是否忠于计划"                                                                       |
| 6   | **Reviewer 和 Paper Agent 同模型** | 和 AI Scientist v1 一样——Self-Review 盲区问题                                                                                                  |
| 7   | **AgentRxiv 的安全和滥用问题未讨论**      | 允许 AI 代理自动上传论文到公共平台——如果代理生成的是错误的、危险的或抄袭的内容，谁来负责？                                                                                        |

### 7.12 Robin

| #   | 缺点                                     | 说明                                                                        |
| --- | -------------------------------------- | ------------------------------------------------------------------------- |
| 1   | **硬件依赖性极强**                            | 需要机器人实验室（液体处理机器人、培养箱、检测设备）。不是纯软件系统——硬件成本可能是数十万到百万美元级别，限制普及                |
| 2   | **实验类型受限**                             | 目前只能做机器人实验室能自动化的湿实验类型（如液体处理、细胞培养）。需要复杂手工操作的实验（如动物实验、手术）无法执行               |
| 3   | **质量验证集中在计算层—湿实验层的异常检测有限**             | 机器人可能执行了 protocol 但实际反应未发生（如试剂过期、污染）——这些物理层面的失败可能不被实时检测到                  |
| 4   | **Protocol Agent 受限于已知实验 protocol 模板** | 和 AI Scientist 的模板依赖类似。真正新颖的实验方法可能需要人类科学家来设计                              |
| 5   | **安全性讨论不足**                            | 全自动生成和执行湿实验——如果 Agent 设计了一个产生有毒副产物的反应，是否有安全拦截机制？论文对此讨论有限                  |
| 6   | **200x 加速可能高估**                        | 2 小时 vs 数月——这是将"AI 的执行时间"与"人类的工作日历时间"比较。如果算上人类的实际工作时间（每天 8 小时），加速倍数可能显著降低 |
| 7   | **单实验室验证**                             | 只在 FutureHouse 的自己的实验室验证过。在其他机器人实验室平台上的可移植性未知                             |

### 7.13 ERA

| #   | 缺点                                   | 说明                                                                                                 |
| --- | ------------------------------------ | -------------------------------------------------------------------------------------------------- |
| 1   | **依赖人类科学家写 spec**                    | 系统的质量上限由人类 spec 的质量决定。如果 spec 有歧义或遗漏关键约束，生成的代码可能在"通过所有测试"的同时仍有问题                                   |
| 2   | **Spec → Acceptance Test 的转化依赖 LLM** | LLM 转化 spec 为测试时可能遗漏边界条件或误解科学约束。如果转化出的测试不完整，代码通过了不完整的测试仍然可能有问题                                     |
| 3   | **只解决代码生成，不解决实验设计**                  | ERA 解决的是"给定 spec 写代码"，不是"给定研究目标设计实验方案"。它是一段 pipeline 而非端到端系统——需要与 Co-Scientist 或 AI Scientist 组合使用 |
| 4   | **进化式生成的停滞风险**                       | 如果 acceptance test 太严格，可能所有候选都无法通过，进化停滞。如果太宽松，低质量代码通过。平衡点需要人类调整                                    |
| 5   | **Acceptance test 只覆盖可测试的属性**        | 物理合理性、数值稳定性、结果可解释性——这些主观维度不在 acceptance test 的覆盖范围内                                                |
| 6   | **未开源**                              | Google 未公开 ERA 的代码，只有论文描述                                                                          |

### 7.14 Materials Lab MAS

| #   | 缺点                                | 说明                                                                           |
| --- | --------------------------------- | ---------------------------------------------------------------------------- |
| 1   | **领域高度特定**                        | 针对材料科学实验室设计——设备类型、实验流程、表征方法都是材料领域绑定，难以直接迁移到其他实验科学领域                          |
| 2   | **Heartbeat 异常检测的误报/漏报问题**        | 心跳频率、异常阈值——这些关键参数没有给出调优方法和敏感性分析。过于敏感的 heartbeat 会产生大量误报，过于迟钝则漏过真实问题          |
| 3   | **Job Shop Scheduling 在复杂依赖下的局限** | 当实验之间有复杂的先后依赖（"实验 A 的材料必须用于实验 B 的表征"），简单的资源锁不够，需要完整的 DAG 管理                  |
| 4   | **多 Agent 协调的成本**                 | Agent 之间频繁的 heartbeat 通信、资源锁竞争、异常处理——这些协调开销在 Agent 数量增加时可能成为瓶颈               |
| 5   | **实验规模相对较小**                      | 与 HTP Screening 的 1000+ 并发任务相比，Materials Lab 的实验规模受限于物理设备数量（通常 <10 台）        |
| 6   | **未讨论 Agent 间的信任和冲突解决**           | 如果两个 Agent 对同一个资源的分配有冲突（如 Agent A 认为需要重做实验，Agent B 认为结果可用），谁来决定？论文没有描述冲突解决机制 |

### 7.15 HTP Screening

| #   | 缺点                             | 说明                                                                                             |
| --- | ------------------------------ | ---------------------------------------------------------------------------------------------- |
| 1   | **HPC 依赖性**                    | 需要 Frontier 级别的超算资源。普通研究组无法使用。系统的价值依赖于对稀缺资源的访问权                                                |
| 2   | **任务类型受限**                     | 目前只覆盖 DFT、MD 等材料计算任务。实验科学任务（湿实验、物理测量）不在覆盖范围内                                                   |
| 3   | **Supervisor 的优先级决策依赖 LLM 判断** | LLM 决定"哪个材料更高优先级"是基于什么标准？能源材料 vs 基础科学——这些优先级判断隐含价值判断，LLM 是否有能力做？缺乏透明的优先级框架                     |
| 4   | **错误处理粗糙**                     | 重试 3 次 → 标记 → 人工通知。没有深入诊断"为什么这个任务一直失败"，没有自适应修复。调试能力不如 ATHENA 的 Debugger 或 AI Scientist 的 AIDER |
| 5   | **后处理校验规则由人预设**                | 能量收敛 < 1e-6 eV、力收敛 < 0.01 eV/A——这些阈值是人工设定的。对于新类型的材料，合适的阈值可能不同，但系统不会自动适配                        |
| 6   | **单点故障风险**                     | Supervisor Agent 是中心化调度器。如果 Supervisor 失败，1000+ 任务的调度可能全部中断。缺乏容错或冗余机制                          |
| 7   | **arXiv 预印本，未正式发表**            | 未经过正式同行评审                                                                                      |

---

### 7.16 跨系统共性缺点

除了各系统的个别缺点外，15 个系统共享以下几个结构性弱点：

| #   | 共性缺点                       | 影响范围                                   | 说明                                                                                                                                               |
| --- | -------------------------- |:--------------------------------------:| ------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | **LLM 知识天花板**              | 全部 15 个                                | 所有系统的"智能"上限最终受限于底层 LLM 的领域知识。如果 LLM 在某个子领域知识不足（如不知道最新的实验技术或数值方法），系统会系统性地在该领域失败。ATHENA 用 Conceptual Scaffolding 缓解了这个问题，但 Scaffolding 本身仍是人工编码的知识 |
| 2   | **可复现性危机**                 | AI Scientist, Agent Lab, DeepScientist | 多个系统报告了 AI 生成的论文和实验结果，但只有少数经过了独立的人工复现验证。Grounded Scrutiny 发现相当比例的已发表论文无法被 AI 复现——这个发现同样适用于 AI 自己生成的论文                                            |
| 3   | **领域迁移困难**                 | 全部 15 个                                | 所有系统都在一个或少数几个领域上验证（ML/NLP/药物/材料）。将系统迁移到新领域需要大量的人工工程（写模板、适配数据格式、定义验证规则、构建知识图谱）。没有一个系统能做到"零人工迁移"                                                   |
| 4   | **成本与质量的 trade-off 未被系统化** | 全部 15 个                                | 目前只有 AI Scientist Nature 论文提出了 R²=0.517 的 Scaling Law。其他系统没有回答"多花 10 倍 compute 能多获得多少科学价值"这个根本性问题。预算管理仍是启发式的                                     |
| 5   | **缺乏负结果的价值利用**             | 除 Sibyl H7 外的全部                        | 科学中负结果（"这个方法不行"）和正结果有同等价值。但大多数系统只奖励"有效"的结果，不奖励"证明什么无效"的结果。这导致搜索空间被重复无效地探索                                                                        |
| 6   | **自主性 vs 可控性的张力未解决**       | 全部                                     | 越自主 → 越高效但也越危险（Robin 可以在无人监督下运行 2 小时做湿实验）。越可控 → 越安全但人工负担越大。目前没有系统在这个光谱上提出明确的定位和 safety guarantee                                                 |
